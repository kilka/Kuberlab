apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-api-egress
  namespace: ocr
spec:
  podSelector:
    matchLabels:
      app: ocr-api
  policyTypes:
  - Egress
  egress:
  # Allow DNS
  - to:
    - namespaceSelector: {}
    ports:
    - protocol: UDP
      port: 53
  # Allow HTTPS to Azure services
  - ports:
    - protocol: TCP
      port: 443
  # Allow AMQPS for Service Bus
  - ports:
    - protocol: TCP
      port: 5671